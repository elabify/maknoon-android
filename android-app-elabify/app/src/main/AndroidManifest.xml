<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Posture: NFC (passport) + biometric required; BLE/USB for hardware
         wallets; camera for QR. No GMS / Play Integrity / location. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.NFC" />
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Mini-app haptics (the bridge's haptic.fire). Without this the Vibrator
         call throws SecurityException, and because the call site wraps it in
         runCatching the failure was silent: the API has never actually vibrated.
         A normal permission, so no runtime prompt and no privacy surface. -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />

    <!-- The hardware-wallet core AARs declare ACCESS_FINE_LOCATION with
         maxSdkVersion=30 for pre-Android-12 BLE scanning (correct for a general
         library). This app's minSdk is 33, so it is never needed and we never use
         location: strip it from the merged manifest so it does not appear in the
         Play listing. BLE scanning here uses BLUETOOTH_SCAN/neverForLocation. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"
        tools:node="remove" />

    <uses-feature android:name="android.hardware.nfc" android:required="false" />
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="false" />
    <uses-feature android:name="android.hardware.usb.host" android:required="false" />
    <uses-feature android:name="android.hardware.camera.any" android:required="false" />

    <application
        android:name=".MaknoonApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="Maknoon"
        android:supportsRtl="true"
        android:localeConfig="@xml/locales_config"
        android:theme="@style/Theme.Maknoon">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="Maknoon"
            android:launchMode="singleTop"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden|uiMode"
            android:theme="@style/Theme.Maknoon">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- WalletConnect deep links (ADR-0049): a dApp hands off a `wc:`
                 pairing URI; `maknoon://wc` is the return-redirect. EVM-only. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="wc" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="maknoon" android:host="wc" />
            </intent-filter>
        </activity>

        <!-- Shares composed images (the passport Share picture) via a content
             URI scoped to the app's own cache "shares" dir. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
